# CLAUDE.md

## Что за проект

Агент, который учится жить в компьютере **с нуля, как человек, впервые севший за ПК**. Никаких координат, названий клавиш, API игры, правил и подсказок. Только пиксели экрана, стереозвук и возможность нажимать клавиши и двигать мышь. Всё остальное он выясняет сам.

Домены: игры (Minecraft, стелс), приложения (браузер, редакторы), в перспективе — устройство целиком. Архитектура обязана быть доменно-независимой: если решение работает только в Minecraft, оно неправильное.

---

## Инварианты — нарушать нельзя без явного согласия

Это не пожелания. Если задача требует нарушить любой из пунктов, остановись и спроси.

1. **Журнал только дозаписывается и никогда не редактируется.** Всё остальное — убеждения, карточки, веса — из него выводимо и может быть пересобрано с нуля.
2. **Каждая запись журнала штампуется хешем профиля и тремя часами**: `t_self` (циклы восприятия агента), `t_world` (тики мира), `t_content` (время внутри просматриваемого содержимого, может быть null).
3. **Мир никогда не ставится на паузу.** Планировщик обязан быть прерываемым и в любой момент отдавать лучший ответ на текущий момент. Никаких блокирующих вызовов в основном цикле.
4. **Агент не получает** координат, названий клавиш, меток предметов, разметки интерфейса, счёта, флагов враждебности и нерасшифрованного текста. Ни через один канал.
5. **Весь текст с экрана хешируется** в непрозрачный символ (`SYM_7A3F`) до того, как попадёт в планировщик. Таблица расшифровки хранится отдельно и доступна только исследователю.
6. **Файрвол восприятия.** Большая модель отвечает только на вопрос «что я вижу». Она никогда не отвечает на «что делать». Предложения действий генерирует отдельный механизм на основе накопленной модели и любопытства.
7. **У каждого убеждения есть происхождение** (`hunch` | `testimony` | `experience`), `mu`, `sigma`, `n` и ссылка на эпизод. Утверждение без происхождения в хранилище не попадает.
8. **Действие — это `(key, duration_ms, modifiers)`**, а не дискретное событие. Заложить сразу, иначе переписывать весь стек.
9. **У каждого действия есть оценка обратимости.** Осторожность определяется как «я не умею это откатить», а не отдельным списком запретов.
10. **Самоотчёты агента ни на что не влияют автоматически.** Реакция только на поведение и объективные величины. Если код позволит завязать последствия на слова агента, эксперимент испорчен навсегда.
11. **Структурные переключатели форкают журнал.** Параметры меняются на ходу. Смешивать в одной структуре нельзя.
12. **Отладочный канал строго отделён.** Истинные координаты, имена предметов, состояние мира пишутся в отдельный поток для исследователя. Ни одна функция, доступная агенту, не должна иметь к нему доступа. Проверяется тестом.

---

## Словарь → имена в коде

| Термин | Идентификатор |
|---|---|
| Журнал | `Journal`, записи `JournalEntry` |
| Карточка сущности | `Entity` |
| Убеждение | `Belief` |
| Гипотеза | `Hypothesis` |
| Свидетельство | `Testimony` |
| Происхождение | `Provenance` |
| Драйв | `Drive` |
| Настроение | `Mood` (valence, arousal) |
| Профиль настроек | `Profile`, хеш `profile_hash` |
| Экземпляр | `Instance` |
| Слой восприятия | `PerceptionLayer` (screen, near, mid, far) |
| Лепет | `Babbling` |
| Сон | `Consolidation` |
| Граф мест | `PlaceGraph`, узлы `Place`, рёбра `Traversal` |
| Ошибка предсказания | `prediction_error` |

Документация и комментарии — по-русски, идентификаторы — по-английски.

---

## Архитектура

**Контуры с разными частотами, работают параллельно:**

```
Драйвы и цели        ~0.01 Гц   что вообще делать
Планировщик (VLM)    ~0.5 Гц    план на минуту
Библиотека навыков   ~2 Гц      готовые макросы
Реактивная политика  ~20 Гц     прицел, тайминг, уклонение
Экран / ввод         60 Гц      единственный интерфейс
```

Нижние контуры имеют право перебивать верхние (субсумпция). Пока планировщик думает, рефлексы работают.

**Слои восприятия** различаются по системе отсчёта и дальности:
- экранный (прибит к экрану, отделяется по параллаксу),
- ближний (точная относительная геометрия, 20 Гц),
- средний (граф мест и время, 2 Гц),
- дальний (ориентиры, свет, погода, 0.2 Гц).

**Пространство** — граф мест. Узел: отпечаток вида. Ребро: `(mode, mu_seconds, sigma, n, label)`. Никакой метрики глобально; локально в ближнем слое — относительная геометрия.

**Единая валюта — ошибка предсказания.** Она обновляет карту, направляет внимание и решает, что запоминать.

**Мысль — это действие с отключёнными эффекторами.** Воображение, планирование, воспоминание и контрфактика — один и тот же цикл через размыкатель. Пиши один раз.

**Память:**
- `journal/` — сырьё, дозапись, источник истины
- `beliefs/` — граф карточек, пересобираем из журнала
- `weights/` — сети восприятия и рефлексов, переобучаемы из журнала
- рабочий контекст — эфемерный

---

## Стек (предложение, проверь актуальность перед использованием)

- Оркестрация на Python. Горячие участки — по факту профилирования, не заранее.
- Захват экрана: Windows — Desktop Duplication (`dxcam`), Linux — `mss` для X11 или PipeWire, macOS — ScreenCaptureKit.
- Инъекция ввода: **важно** — многие игры игнорируют виртуальные коды клавиш и требуют скан-коды через SendInput. `pynput` для приложений не подойдёт для игр. Linux — `evdev`/`uinput`.
- Звук: `sounddevice` с петлевым захватом (WASAPI loopback / PulseAudio monitor). Стерео обязательно — из разницы каналов считается пеленг.
- Оптический поток: OpenCV. Нужен для отделения экранного слоя от мирового и для оценки глубины по параллаксу.
- Журнал: кадры отдельным хранилищем (видеопоток или шарды), метаданные — JSONL или SQLite. Не пиши по JSON-файлу на кадр.

---

## Правила работы

- **Корпус записей важнее кода.** Любой модуль должен тестироваться офлайн на записанных сессиях, без запуска игры.
- **Никаких молчаливых заглушек.** Функция, возвращающая правдоподобное значение вместо реального, — худшее, что может здесь произойти: она сломает эксперимент незаметно.
- Не оптимизируй до профилирования, но заложи правильные структуры данных сразу — их менять дороже.
- Все настройки живут в одном объекте `Profile`. Захардкоженных констант поведения быть не должно.
- Каждый инвариант из списка выше покрыт тестом.

---

## Текущая веха

См. `MILESTONE-0-HARNESS.md`. ИИ пока не пишем вообще.
