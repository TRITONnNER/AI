"""То, и только то, что доступно агенту.

Инвариант 12 в виде правила импорта: ничто, достижимое отсюда, не имеет права
импортировать `harness.debug`. Проверяется `tests/test_invariant_12_isolation.py`
обходом графа импортов, а не договорённостью.

Поэтому здесь нет ни журнала, ни сессии, ни корпуса: агенту незачем читать
хранилище опыта целиком — он получает восприятие и отдаёт действия. Пересборка
убеждений из журнала — работа исследовательской стороны, и она живёт вне этого
пакета.

В вехе 0 агента нет. Пакет существует, чтобы граница была проведена и проверена
до того, как появится первый агент, — потом её проводить будет некуда.
"""

from .body import Body
from .perception import Percept

__all__ = ["Body", "Percept"]
