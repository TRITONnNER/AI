"""Харнесс вехи 0: запись и воспроизведение опыта. Ни одной модели.

Что где лежит:

- `harness.core`      — часы, профиль, действие, символы, журнал, хранилища.
- `harness.capture`   — источники кадров и звука.
- `harness.inject`    — инъекция ввода, маска, СТОП, сторожевой таймер.
- `harness.replay`    — покадровое воспроизведение записанной сессии.
- `harness.vision`    — разделение экранного и мирового слоёв (0.6).
- `harness.corpus`    — генератор синтетического корпуса с известной истиной.
- `harness.agentside` — то, и только то, что доступно агенту.
- `harness.debug`     — поток исследователя: истина, координаты, расшифровка.

Последние два не должны иметь между собой пути импорта ни в одну сторону — это
инвариант 12, и он проверяется тестом, а не дисциплиной.
"""

__version__ = "0.1.0"
